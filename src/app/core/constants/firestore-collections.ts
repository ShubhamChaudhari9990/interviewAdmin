/** Firestore collection + document id for career selection master data. */
export const INTERVIEW_DOMAIN_COLLECTION = 'interview_domain';

/** Firestore collection for subscription plan documents (free, pro, enterprise). */
export const PLANS_COLLECTION = 'plans';

/** Firestore collection for FAQ category documents (each holds a questions array). */
export const FAQS_COLLECTION = 'faqs';
